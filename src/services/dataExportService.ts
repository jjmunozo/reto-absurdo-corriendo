// Placeholder para mantener compatibilidad con archivos read-only

export const isAdminMode = () => false;
export const exportData = () => {
  throw new Error('Sistema actualizado: funcionalidad obsoleta');
};
